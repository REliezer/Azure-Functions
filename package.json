{
  "name": "backend-azure",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start node dist/index.js",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "bcrypt": "^5.1.1",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/mssql": "^9.1.7",
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "typescript": "^5.7.3"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
